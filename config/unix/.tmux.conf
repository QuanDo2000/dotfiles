# Enable true color support. Use a terminal-agnostic RGB capability so 24-bit
# color works regardless of the outer terminal's TERM (e.g. ghostty reports
# xterm-ghostty, which a TERM-specific override like xterm-256color:RGB misses,
# downgrading colors to 256 — washed-out catppuccin).
set -as terminal-features ",*:RGB"
# Keep Pi's clear-and-redraw updates atomic to prevent visible flashing.
set -as terminal-features ",*:sync"

# Claude configs
set -g allow-passthrough on
set -s extended-keys on
set -g extended-keys-format csi-u
set -as terminal-features 'xterm*:extkeys'

# Enable OSC 52 clipboard
set -g set-clipboard on
bind -T copy-mode-vi y send-keys -X copy-selection-and-cancel
bind -T copy-mode-vi Y send-keys -X copy-selection-and-cancel \; paste-buffer -p
bind -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-selection-and-cancel

set -g display-time 4000
set -g status-interval 5
bind C-p previous-window
bind C-n next-window
bind b last-window
bind R run-shell 'tmux source-file ~/.config/tmux/tmux.conf > /dev/null; tmux display-message "Sourced tmux.conf!"'

# --- tmux-pain-control (inlined; formerly tmux-plugins/tmux-pain-control) ---
# Pane navigation
bind h select-pane -L
bind C-h select-pane -L
bind j select-pane -D
bind C-j select-pane -D
bind k select-pane -U
bind C-k select-pane -U
bind l select-pane -R
bind C-l select-pane -R
# Window reordering
bind -r "<" swap-window -d -t -1
bind -r ">" swap-window -d -t +1
# Pane resizing
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5
# Splits and new windows open in the current path
bind "|" split-window -h -c "#{pane_current_path}"
bind "\\" split-window -fh -c "#{pane_current_path}"
bind "-" split-window -v -c "#{pane_current_path}"
bind "_" split-window -fv -c "#{pane_current_path}"
bind "%" split-window -h -c "#{pane_current_path}"
bind '"' split-window -v -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"

# Catppuccin Macchiato theme, inlined from catppuccin/tmux 2.1.3.
set -g status-style "default"
set -g mode-style "bg=#363a4f,bold"
set -g message-style "fg=#8bd5ca,bg=default,align=centre"
set -g message-command-style "fg=#8bd5ca,bg=default,align=centre"
set -g pane-border-style "fg=#6e738d"
set -g pane-active-border-style "#{?pane_in_mode,fg=#b7bdf8,#{?pane_synchronized,fg=#c6a0f6,fg=#b7bdf8}}"
set -g clock-mode-colour "#8aadf4"
set -g window-status-activity-style "bg=#b7bdf8,fg=#181926"
set -g window-status-bell-style "bg=#eed49f,fg=#181926"
set -g window-status-separator " "
set -g window-status-format "#[fg=#181926,bg=#939ab7] #I #[fg=#cad3f5,bg=#363a4f] #{b:pane_current_command} "
set -g window-status-current-format "#[fg=#181926,bg=#c6a0f6] #I #[fg=#cad3f5,bg=#494d64] #{b:pane_current_command} "

set -g status-right-length 100
set -g status-left ''
set -g status-right '#[fg=#f4dbd6]#[fg=#181926,bg=#f4dbd6] #[fg=#cad3f5,bg=#363a4f] #{b:pane_current_path}#[fg=#363a4f] #[fg=#91d7e3]#[fg=#181926,bg=#91d7e3] #[fg=#cad3f5,bg=#363a4f] #(whoami)#[fg=#363a4f] #[fg=#c6a0f6]#[fg=#181926,bg=#c6a0f6]󰒋 #[fg=#cad3f5,bg=#363a4f] #H#[fg=#363a4f] #[fg=#7dc4e4]#[fg=#181926,bg=#7dc4e4]󰃰 #[fg=#cad3f5,bg=#363a4f] %Y-%m-%d %H:%M:%S#[fg=#363a4f] '
