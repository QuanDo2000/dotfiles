model = "gpt-5.5"
model_reasoning_effort = "medium"

# ponytail: last_updated omitted — codex stamps it on `marketplace upgrade`
[marketplaces.ponytail]
source_type = "git"
source = "https://github.com/DietrichGebert/ponytail.git"

[plugins."ponytail@ponytail"]
enabled = true

# superpowers ships in the built-in openai-curated marketplace, so no
# [marketplaces.*] entry is needed — only the plugin cache (populated by
# `codex plugin add` in extras.sh, gated on `codex login status` succeeding
# AND the openai-curated marketplace being cloned, which requires running
# `codex` interactively at least once after login).
[plugins."superpowers@openai-curated"]
enabled = true

[projects."/Users/quando/dotfiles"]
trust_level = "trusted"

[hooks.state]

[hooks.state."ponytail@ponytail:hooks/claude-codex-hooks.json:session_start:0:0"]
trusted_hash = "sha256:195e73e498a4cdb40bbe765719d39371ac72c0ebb084fee1beee9d28d21fb7a0"

[hooks.state."ponytail@ponytail:hooks/claude-codex-hooks.json:user_prompt_submit:0:0"]
trusted_hash = "sha256:f79c8de133c1109b841f9835a9d032231900a0d96ecf5f6d1866132df5ca4aa4"

[tui.model_availability_nux]
"gpt-5.5" = 2
