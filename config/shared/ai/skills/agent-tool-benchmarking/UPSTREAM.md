# Provenance and maintenance

Promoted on 2026-09-22 from the locally maintained default Hermes profile's
`skills/evaluation/agent-tool-benchmarking/`. The source had no author, license,
or upstream revision fields; none have been invented. This is a user-approved
local workflow publication, not a verified upstream package or license claim.

The complete entry point and all 19 references are retained. Promotion aligns
Codex-home isolation, credential opacity, and experiment-owned cleanup with the
owning skill's existing approval rules. Historical benchmark figures and
version-scoped observations are inherited examples; their raw artifacts are not
included and their empirical results were not revalidated during promotion.
Do not treat them as evidence about current tools or a new user's workloads.

Assumptions: explicitly approved spend, pinned runtime/tool versions, supported
isolation, disposable repositories, and authoritative correctness/accounting
checks. Linux namespace/cgroup examples need native equivalents on other
platforms; do not silently weaken containment. Model names, prices, seven-trial
preferences, and weighting examples are not an override of the current user's
bounded experiment contract. No benchmark is authorized by installing this skill.

Manual maintenance only: edit this tracked directory, not installed links or
copies. Repository ownership: `config/shared/ai/skills/README.md`. Credentials, sessions,
databases, raw benchmark events, and private source repositories are not bundled.
