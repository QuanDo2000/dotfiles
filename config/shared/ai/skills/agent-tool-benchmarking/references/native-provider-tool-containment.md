# Credential-opaque provider with sandboxed tools

- Separate the credential-bearing native provider process from every model-selected file/terminal execution. Correct cwd and output redaction do not establish containment or undo prior provider exposure.
- Use an installed namespace sandbox with private mounts, PID and network namespaces, closed inherited descriptors, no capabilities, and a fresh allowlisted subprocess environment. Mount only one disposable workspace writable and necessary runtimes read-only; never mount a whole home or copy credentials.
- Run the native tool dispatcher inside the boundary so file-operation helper reads, document extraction, linting, terminal subprocesses and approval guards share the same containment. Preserve approval guards; include their required runtime modules instead of bypassing missing-import failures.
- Deny every tool outside the tested allowlist at dispatch as well as schema exposure. Disable benchmark host-side file checkpoints/dynamic path-derived hints that could perform reads outside the sandbox. Verify actual native-agent cwd and schemas provider-free.
- Prove the boundary with a synthetic readable host sentinel outside the workspace: absolute, traversal and symlink reads; search/patch/write attempts; sentinel integrity; injected fake inherited credential; procfs visibility; and legitimate edits/tests. Positively verify protected-instruction denial and network isolation. Never use real secrets as canaries.
- Capture response-completed usage metadata for every observed native response request, including auxiliary and grace calls. Store only usage and routing metadata, never headers or request bodies. Reconcile native exclusive-input counters with wire inclusive-input/cached details and database totals; keep historical missing categories explicit.
- Execute enumerated added test files rather than assuming root unittest discovery traverses non-package subdirectories. Reject zero-test success. Keep hidden acceptance fixed when correcting an offline auditor invocation.
- After a quarantined pilot, preserve original ceiling, baseline and excluded costs. A fresh retry-start meter is only an incremental guard; verify historical window identity before applying the original cumulative ceiling. Gate the second cell on substantive first-cell patch/test, scope, privacy and accounting validity.
