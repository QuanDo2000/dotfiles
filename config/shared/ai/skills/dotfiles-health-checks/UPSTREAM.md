# Provenance and maintenance

Promoted on 2026-09-22 from the default Hermes profile's
`skills/devops/dotfiles-health-checks/` after the authorized local guidance cleanup.
The source frontmatter credits **Hermes Agent** and declares **MIT**; those fields
are retained. No independently pinned upstream repository/revision was present in
this local package, so this is a local adaptation, not an upstream release claim.

The complete reference tree is retained. The entry point routes specialized
procedures rather than loading every maintenance workflow. Promotion replaces a
private SSH host with `server.example.invalid`, gates historical attribution on
bounded user-approved sources, and aligns native-package migration with the
existing independent-bootstrap-need rule.

Assumptions: Home Manager/Nix, role-aware repository activation, and optional
systemd, PowerShell, editor, or agent tooling depending on the selected reference.
Host roles, service names, paths, package versions, and CLI examples are examples
to resolve against the current repository—not authority to activate, install,
read history, or perform privileged maintenance on a new host.

Manual maintenance only: edit this tracked directory, not installed links or
copies. Repository ownership: `config/shared/ai/skills/README.md`. No runtime state, credentials,
or conversation exports are included.
