# Provenance and maintenance

Promoted on 2026-09-22 from the default Hermes profile's
`skills/github/github-pr-workflow/` after the authorized local guidance cleanup.
The source frontmatter credits **Hermes Agent** and declares **MIT**; those fields
are retained. No independently pinned upstream repository/revision was present in
this local package, so this is a local adaptation, not an upstream release claim.

The complete skill, references, and PR templates are retained. The adaptation
uses scoped staging, required signing, verified remote identity, exact-head CI,
and governing publication/merge authority. `gh`, a configured Git identity, and
repository-native checks must already be available. Templates are examples, not
proof of tests or issue-closing authority.

Manual maintenance only: edit this tracked directory, not installed links or
copies. Repository ownership: `config/shared/ai/skills/README.md`. No credentials, runtime state,
private host addresses, or conversation exports are part of this package.
